{
  "name": "cf-worker-verified-email-router",
  "version": "0.1.0",
  "private": true,
  "description": "send email to Verified Destination Address",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260904.1",
    "typescript": "^5.9.2",
    "vitest": "^4.0.0",
    "wrangler": "^4.39.0"
  }
}
